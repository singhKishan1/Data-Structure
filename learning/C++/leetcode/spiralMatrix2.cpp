#include <iostream>
#include <vector>

using namespace std;

void solve(int rows, int cols, int rStart, int cStart){
    vector<vector<int>>(rows, vector<int>(cols,0));
    int i=rStart,j=cStart;
    
}

int main(){
    int rows,cols,rStart,cStart;
    cin>>rows>>cols>>rStart>>cStart;

    solve(rows,cols, rStart, cStart);
    return  0;
}
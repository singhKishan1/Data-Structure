#include <iostream>
#include <vector>

using namespace std;



int solve(vector<int> &v)
{
    int n
}

int main()
{
    int n;
    cin >> n;
    vector<int> v(n);
    for (int i = 0; i < n; ++i)
        cin >> v[i];

    vector<int>dp(1001,1001);

    cout << solve(v) << endl;
    return 0;
}
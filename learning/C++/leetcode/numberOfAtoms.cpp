#include <iostream>
#include <stack>
#include <algorithm>
#include <map>

using namespace std;

string solve(string formula){
    map<string, int> mp;

    stack<char> st;
    for(auto x:formula){
        if(x>='A' && x<='Z'){
            
        }
    }
    
    
    string ans = "";
    return ans;
}

int main(){
    string formula;
    cin>>formula;

    cout << solve(formula) << endl;
    return 0;
}
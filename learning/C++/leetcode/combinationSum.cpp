#include <iostream>
#include <vector>

using namespace std;

vector<vector<int>> dp(50);

vector<int> v solve(vector<int>& v,int sum, int target){
    if(sum==target){
        
    }
}

vector<vector<int>> combinationSum(vector<int>& v, int target){
    
}

int main(){
    int n;
    cin>>n;
    vector<int> v(n);
    for(int i=0; i<n; ++i)
        cin>>v[i];

    int target;
    cin>>target;

    vector<vector<int>> ans = combinationSum(v, target);

}